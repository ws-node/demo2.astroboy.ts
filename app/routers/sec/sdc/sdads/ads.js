// [astroboy.ts] 自动生成的代码
const CTOR = require("../../../../controllers/sec/sdc/sdads/ads");
const { buildRouter } = require("astroboy.ts");
module.exports = buildRouter(CTOR, "sec.sdc.sdads.ads", "/v1");